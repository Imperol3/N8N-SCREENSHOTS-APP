services:
  n8n-scraper:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    # Optimize for browser
    shm_size: '1gb'
    cap_add:
      - SYS_ADMIN
    init: true
